# 🎯 Настройка MCP для Cursor

## Шаг 1: Открыть настройки Cursor

1. Откройте **Cursor Settings** (Ctrl+,)
2. Найдите раздел **"MCP Servers"** или **"Model Context Protocol"**
3. Нажмите **"Add Server"** или **"Edit Config"**

## Шаг 2: Добавить конфигурацию

Добавьте следующую конфигурацию:

```json
{
  "mcpServers": {
    "confluence-rag": {
      "command": "python",
      "args": [
        "-u",
        "C:\\MCP\\confluence-rag-project\\cursor_mcp_client.py"
      ],
      "env": {
        "MCP_SERVER_URL": "http://localhost:8012/mcp"
      }
    }
  }
}
```

**ВАЖНО:** Убедитесь что путь `C:\\MCP\\confluence-rag-project\\cursor_mcp_client.py` правильный!

## Шаг 3: Перезапустить Cursor

1. Сохраните настройки
2. Перезапустите Cursor (полностью закройте и откройте снова)

## Шаг 4: Проверить что MCP работает

1. Откройте Cursor Chat (Ctrl+L)
2. Напишите: `@confluence-rag технологический стек RAUII`
3. Должны появиться результаты поиска

## 🧪 Тестовые запросы

```
@confluence-rag технологический стек RAUII
@confluence-rag уточняющие вопросы для обследования
@confluence-rag список всех spaces
```

## 🔧 Troubleshooting

### Проблема: "MCP server not available"

**Решение:**
1. Проверьте что контейнер запущен: `docker-compose ps`
2. Проверьте что порт 8012 доступен: `curl http://localhost:8012/mcp`
3. Проверьте логи: `docker-compose logs confluence-rag`

### Проблема: "Connection refused"

**Решение:**
1. Убедитесь что `confluence-rag` контейнер healthy: `docker-compose ps`
2. Перезапустите контейнер: `docker-compose restart confluence-rag`

### Проблема: "Python not found"

**Решение:**
Измените `"command": "python"` на полный путь:
- Windows: `"C:\\Python313\\python.exe"` (проверьте свой путь)
- Linux/Mac: `"/usr/bin/python3"`

## 📊 Доступные инструменты

После подключения MCP в Cursor Chat будут доступны:

- `confluence_semantic_search` - Семантический поиск по Confluence
- `confluence_search_by_label` - Поиск по меткам
- `confluence_list_spaces` - Список пространств
- `confluence_health` - Проверка здоровья системы

## 💡 Примеры использования

### Простой поиск
```
@confluence-rag найди информацию про технологический стек
```

### Поиск с фильтром по space
```
@confluence-rag search "архитектура проекта" in RAUII space
```

### Поиск по метке
```
@confluence-rag найди все страницы с меткой "important"
```

