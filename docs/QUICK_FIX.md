# Быстрое исправление mcp_rag_secure.py

## ✅ Что сделано

1. ✅ Создан `.flake8` - конфигурация линтера
2. ✅ `pyproject.toml` - уже существует с настройками Black/isort
3. ✅ `docs/refactoring_plan.md` - план рефакторинга сложных функций

## 🔧 Что нужно сделать СЕЙЧАС (5 минут)

### Шаг 1: Удаление trailing whitespace (430 строк)

**В VS Code:**
1. Открой `mcp_rag_secure.py`
2. `Ctrl+H` (Find & Replace)
3. Включи regex (кнопка `.*`)
4. **Find:** `\s+$`
5. **Replace:** `` (оставь пустым)
6. **Replace All**

**Результат:** 430 ошибок исправлено ✅

---

### Шаг 2: Удаление неиспользуемого импорта `Tuple`

**Найди строку 5:**
```python
from typing import Optional, Tuple, Any, List, Dict
```

**Замени на:**
```python
from typing import Optional, Any, List, Dict
```

**Результат:** 1 ошибка исправлена ✅

---

### Шаг 3: Добавление пробелов после запятых (3 случая)

**Строка 317, 764, 765** - найди и добавь пробелы после запятых вручную.

**Результат:** 3 ошибки исправлены ✅

---

## 📊 Итого после исправлений

**Было:** 500 проблем
**Исправлено:** 434 проблемы (87%)
**Осталось:** 66 проблем (49 длинных строк + 17 сложных функций)

**Оставшиеся проблемы:**
- E501 (длинные строки) - **игнорируются** через `.flake8`
- C901 (сложность) - **запланирован рефакторинг** (см. `docs/refactoring_plan.md`)

---

## 🚀 Следующие шаги (опционально)

### Автоформатирование (если установлены инструменты)

```bash
# Установка (если еще не установлено)
pip install black isort flake8

# Автоформатирование
black --line-length=120 rag_server/mcp_rag_secure.py
isort rag_server/mcp_rag_secure.py

# Проверка
flake8 rag_server/mcp_rag_secure.py --max-line-length=120
```

### Pre-commit hooks (для будущих коммитов)

```bash
pip install pre-commit
pre-commit install
```

---

## 📝 Рефакторинг (следующая неделя)

См. детальный план в `docs/refactoring_plan.md`

**Приоритет 1:**
- `get_adaptive_rerank_limit` (complexity 86 → <15)
- `confluence_semantic_search` (complexity 84 → <15)

**Timeline:** 2-3 недели
