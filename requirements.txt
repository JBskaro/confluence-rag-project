# Full installation (all dependencies)
# Install with: pip install -r requirements.txt
# This will install ~3-5GB of dependencies

# Include all requirement files
-r requirements-core.txt
-r requirements-ml.txt
-r requirements-dev.txt

# NOTE: For lighter installation options:
# - Core only (no ML): pip install -r requirements-core.txt (~200MB, 2-3 min)
# - Core + ML: pip install -r requirements-core.txt -r requirements-ml.txt (~3GB, 10-20 min)
# - Core + Dev: pip install -r requirements-core.txt -r requirements-dev.txt (~300MB, 3-5 min)
